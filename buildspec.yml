version: 0.2

phases:
  install:
    commands:
      - aws cloudformation package --template-file template.yaml --s3-bucket ccdb-lambda-pipeline --output-template-file outputTemplate.yml
artifacts:
  files:
    - template.yaml